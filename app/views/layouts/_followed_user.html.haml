.row.user{id: "js-following-#{following.id}"}
  .col-xs-9.name
    = following.followed
  .col-xs-2
    = link_to following_path(id: following.id), method: :delete do
      .btn.sidebar-btn.icon
        e